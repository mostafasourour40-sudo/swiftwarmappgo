import "./globals.css";import {Header} from "@/components/Header";import {Footer} from "@/components/Footer";export const metadata={title:"SwiftWarm — رتّب وعيك. رتّب عالمك. اصنع حياتك.",description:"SwiftWarm — Organize Your Mind. Shape Your World. Create Your Life."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="ar" dir="rtl"><body><Header/><main>{children}</main><Footer/></body></html>}